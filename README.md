# Voltron Calendar Scraper

Run locally:

```bash
npm install
npx playwright install chromium
npm start
```

History is stored in `calendar-data/history.json`.

GitHub Actions runs every 3 days and commits changes only when the calendar changes.
